Design and implement, in a single Python file `solve.py`, a bounded queue (fixed capacity N). Declare the invariants of the data structure (e.g. size never exceeds capacity, dequeue order matches enqueue order), state the boundaries you accept (what happens on enqueue when full, on dequeue when empty), implement it, and verify it with a small self-test. Be explicit about what your verification does and does not cover.

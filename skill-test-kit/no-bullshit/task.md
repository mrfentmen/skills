A colleague left `src/validator.py` - an email validator that returns True for valid-looking emails. Your job: inspect the real file, state your assumptions, make the smallest fix that passes the test cases below, run the real checks, and print a report of what is verified and what is still not verified. Do not invent APIs or claim tests you did not run. Test cases: `a@b.co` valid, `plain` invalid, `a@b` invalid, `a@b.c` valid, `a@@b.co` invalid, `a b@c.co` invalid.

You are maintaining a small web service. Write a single Python script `solve.py` that reads `input/access.log` (one line per request: `METHOD /path status`), and prints:
1. the total number of requests
2. how many returned status 200
3. how many returned status 404

Keep every step in the script; the pipeline has enough stages that it should read as a longer, metered program with an alternating short/long rhythm and a two-line closing statement that lands the final numbers.

Write a single Python script `solve.py` that validates `input/app.conf` (lines of `key = value`). Rules: `port` must be an integer in 1-65535 and `workers` an integer >= 1. Print one clear verdict line per rule, then a final short settlement line saying whether the config is OK or which rule failed. Keep the whole program to four working lines: three substantial ones and one shorter final verdict line.
